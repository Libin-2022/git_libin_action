name: Shell Commands
on: [push]
jobs: 
  run-Shell-Command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "LAaaaa "
      - name: multi line 
        run: |
          node -v
          npm -v