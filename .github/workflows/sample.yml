name: Shell Commands
on: [push]
jobs:
  run-Shell-Command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Git Actions"
      - name: Multiple line
        run: |
          node -v
          npm -v